function [apRobot,possible,step] = GmapCheckRoundTurn(apRobot)
    %{
    test if it is possible to do round turn at the current location
    if not find the closest step
    %}
    possible=false;
    step=[];
    
    
    endfunction